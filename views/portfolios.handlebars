{{> base}}
{{#if info.length}}
<section>
    <h2>Prior portfolios</h2>
    <ol>
        {{#each info as |info|}}
        <li>
            <a href="/portfolios/{{info.id}}" class="portfolio">Portfolio {{info.portfolio_name}}</a>
        </li>
        {{/each}}
    </ol>
</section>
{{/if}}